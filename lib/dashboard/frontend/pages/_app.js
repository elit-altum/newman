// Import the global scss styles
import "../styles/index.scss";

// The default export required in a new `pages/_app.js` file.
export default function MyApp({ Component, pageProps }) {
    return <Component {...pageProps} />;
}
